import { useState } from 'react';
import { Bookmark, Check, ChevronRight, CircleUserRound, Settings, Star } from 'lucide-react';
import { Link } from 'wouter';
import { characters, medals } from '@/data/mockData';
import { CharacterCard, GuideShell, PageIntro, SidebarCard } from '@/components/guide-shell';

export default function MyPage() {
  const [editing, setEditing] = useState(false);
  const [name, setName] = useState('プレイヤー 01');
  return <GuideShell><PageIntro eyebrow="PERSONAL ARCHIVE" title="マイページ" description="ブックマークしたデータと、あなたの攻略メモを管理します。" action={<button onClick={() => setEditing(!editing)} className="flex items-center gap-2 rounded-sm border border-border bg-card px-3 py-2 text-xs font-bold hover:border-primary hover:text-primary" data-testid="button-profile-settings"><Settings size={14} />プロフィール設定</button>} /><div className="grid gap-8 xl:grid-cols-[minmax(0,1fr)_280px]"><div><section className="rounded-md border border-card-border bg-card p-5 shadow-card sm:p-7"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="grid h-16 w-16 place-items-center rounded-sm bg-sidebar text-xl font-black text-white">01</div><div className="flex-1"><div className="data-label">PLAYER PROFILE</div>{editing ? <div className="mt-2 flex gap-2"><input value={name} onChange={(event) => setName(event.target.value)} className="rounded-sm border border-border bg-background px-2 py-1 text-sm font-bold outline-none focus:border-primary" data-testid="input-profile-name" /><button onClick={() => setEditing(false)} className="rounded-sm bg-primary px-3 py-1 text-xs font-bold text-white" data-testid="button-save-profile"><Check size={13} /></button></div> : <h2 className="mt-1 text-lg font-black">{name}</h2>}<p className="mt-1 text-xs text-muted-foreground">データベース閲覧歴 24日 / 最終アクセス 今日</p></div><div className="flex gap-5 border-t border-border pt-4 sm:border-l sm:border-t-0 sm:pl-7 sm:pt-0"><div><span className="data-label">SAVED</span><p className="mt-1 font-data text-xl font-semibold text-primary">08</p></div><div><span className="data-label">NOTES</span><p className="mt-1 font-data text-xl font-semibold">03</p></div></div></div></section><section className="mt-8"><div className="mb-4 flex items-end justify-between"><div><div className="data-label">BOOKMARKS / CHARACTER</div><h2 className="mt-1 text-lg font-black">保存したキャラクター</h2></div><button className="flex items-center gap-1 text-xs font-bold text-muted-foreground hover:text-primary" data-testid="button-view-all-character-bookmarks">すべて見る <ChevronRight size={13} /></button></div><div className="grid gap-3 md:grid-cols-2">{characters.slice(0, 2).map((character) => <CharacterCard key={character.id} character={character} />)}</div></section><section className="mt-8"><div className="mb-4"><div className="data-label">BOOKMARKS / MEDAL</div><h2 className="mt-1 text-lg font-black">保存したメダル</h2></div><div className="grid gap-3 md:grid-cols-2">{medals.slice(0, 2).map((medal) => <div key={medal.id} className="relative"><span className="absolute right-3 top-3 z-10 text-primary"><Bookmark size={14} fill="currentColor" /></span><Link href={`/medals/${medal.id}`} className="block" data-testid={`link-bookmarked-medal-${medal.id}`}><div className="rounded-md border border-card-border bg-card p-4 shadow-card hover:border-primary"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-sm text-white" style={{ background: medal.color }}>印</div><div><p className="text-sm font-black">{medal.name}</p><p className="text-[10px] text-muted-foreground">{medal.effect}</p></div></div></div></Link></div>)}</div></section></div><aside className="space-y-4"><SidebarCard title="活動メモ"><div className="space-y-3"><div className="flex items-center gap-3"><div className="grid h-8 w-8 place-items-center rounded-sm bg-amber-50 text-amber-600"><Star size={15} /></div><div><p className="text-xs font-bold">黒鉄 蘭を閲覧</p><p className="text-[10px] text-muted-foreground">今日 09:42</p></div></div><div className="flex items-center gap-3"><div className="grid h-8 w-8 place-items-center rounded-sm bg-blue-50 text-primary"><Bookmark size={15} /></div><div><p className="text-xs font-bold">月華の調べを保存</p><p className="text-[10px] text-muted-foreground">昨日 18:10</p></div></div></div></SidebarCard><div className="rounded-md border border-primary/20 bg-blue-50 p-4"><CircleUserRound size={20} className="text-primary" /><p className="mt-3 text-xs font-black">攻略を記録する</p><p className="mt-1 text-[11px] leading-5 text-blue-900/70">気になるデータをブックマークして、自分だけの攻略ノートを作りましょう。</p></div></aside></div></GuideShell>;
}